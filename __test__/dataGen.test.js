// unit tests for data generation script
// import fs
const fs = require('fs');
const path = require('path');

describe('Test data generation scripts', () => {
  it('should create a .csv file', () => {
    // check if file is .csv file
  });
});